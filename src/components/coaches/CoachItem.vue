<template>
    <li>
        <h3>{{ fullName }}</h3>
        <h4>${{ rate }}</h4>
        <div>
            <span v-for="area in areas" :key="area">{{ area }}</span>
        </div>
        <div class="actions">
            <router-link :to="coachContactLink"> contact</router-link>
            <router-link :to="coachDetailsLink"> detail</router-link>
        </div>
    </li>
</template>
<script>
export default {
    props: ['id', 'first_name', 'last_name', 'rated', 'areas'],
    computed: {
        fullName() {
            return this.fullName + '' + this.lastName;
        },
        coachContactLink() {
            return this.$route.path + '/'+ this.id + '/contact'
        },
        coachDetailsLink() {
            return this.$route.path + '/'+ this.id
        }
    },
};
</script>
<style scoped>
li {
    margin: 1rem 0;
    border: 1px solid #424242;
    border-radius: 12px;
    padding: 1rem;
}

h3 {
    font-size: 1.5rem;
}

h3,
h4 {
    margin: 0.5rem 0;
}

div {
    margin: 0.5rem 0;
}

.actions {
    display: flex;
    justify-content: flex-end;
}
</style>